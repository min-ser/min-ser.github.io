import Link from "next/link"; import { ContentDocument } from "@/types/content";
export default function ExpertiseSection({doc}:{doc:ContentDocument}){const items=Array.isArray(doc.meta.items)?doc.meta.items as Array<Record<string,string>>:[];const action=(doc.meta.action||{}) as Record<string,string>;return <section className="section capabilitiesSection"><div className="sectionHead"><span>{String(doc.meta.index).padStart(2,"0")} / {String(doc.meta.title||"").toUpperCase()}</span>{action.href&&<Link href={action.href}>// {action.label} ↗</Link>}</div><div className="capabilityGrid">{items.map((item,i)=><div className="capabilityCard" key={`${item.title}-${i}`}><div className="capabilityNo">{String(i+1).padStart(2,"0")}</div><div className="capabilityIcon">⌁</div><h3>{String(item.title).toUpperCase()}</h3><p>{item.description}</p></div>)}</div></section>}
