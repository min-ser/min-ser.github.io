import Link from "next/link"; import { ContentDocument } from "@/types/content";
export default function ProjectSection({config,projects}:{config:ContentDocument;projects:ContentDocument[]}){const visible=projects.slice(0,Number(config.meta.limit||projects.length));const action=(config.meta.action||{}) as Record<string,string>;return <section className="section compactSection"><div className="sectionHead"><span>{String(config.meta.index).padStart(2,"0")} / {String(config.meta.title||"").toUpperCase()}</span>{action.href&&<Link href={action.href}>// {action.label} ↗</Link>}</div><div className="featuredProjectGrid">{visible.map((d,i)=><Link className="featuredProjectCard" href={`/projects/${d.slug}`} key={d.slug}><div className="featuredProjectMeta"><span>{String(i+1).padStart(2,"0")}</span><span>{String(d.meta.startDate||"")}</span></div><h3>{String(d.meta.title||"")}</h3><div className="tags">{Array.isArray(d.meta.skills)&&d.meta.skills.slice(0,4).map(s=><span key={String(s)}>{String(s)}</span>)}</div><b>OPEN ↗</b></Link>)}</div></section>}
