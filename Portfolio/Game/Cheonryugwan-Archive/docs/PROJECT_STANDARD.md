# Project Standard

`archive/` and `story/` are authoritative. `game/` is derived.

Priority: original novel → approved Archive → game data → UI output.
Originals are preserved, uncertain settings are not guessed, and analysis is
stored separately.
