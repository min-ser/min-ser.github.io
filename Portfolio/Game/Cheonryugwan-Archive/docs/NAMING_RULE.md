# Naming Rule

Use lowercase kebab-case where practical: `seo-harin`, `volume-01`,
`chapter-03`, `scene-014`. Use `YYYY-MM-DD` for dates and Semantic Versioning
for releases. Image examples: `profile.webp`, `full-body.webp`.
