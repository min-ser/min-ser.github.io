# v0.8.4.2 버튼 버그 최종 검수

## 실제 원인

`game/engine/novel-engine.js`의 비 음원 교체 과정에서 아래와 같은
중복 구문이 남아 있었습니다.

```text
}catch{}rainNode.disconnect();...
```

브라우저가 JavaScript 파일을 파싱하지 못해 이후 버튼 이벤트가 하나도
등록되지 않았습니다.

## 수정 완료

- [x] 중복 `catch` 구문 제거
- [x] `stopRain()` 함수 재작성
- [x] `novel-engine.js` 구문 검사 통과
- [x] `common.js` 구문 검사 통과
- [x] 홈 CSS·JS 경로 존재 확인
- [x] 게임 CSS·JS 경로 존재 확인
- [x] 홈 링크 대상 경로 존재 확인
- [x] 게임 데이터 JSON 파싱 확인
- [x] ZIP 무결성 확인

## 버튼 검수 대상

- [x] 새로 시작 이벤트 등록 코드
- [x] 이어하기 이벤트 등록 코드
- [x] 저장 불러오기 이벤트 등록 코드
- [x] 설정 이벤트 등록 코드
- [x] 제목 화면 복귀 이벤트 등록 코드
- [x] 소리 테스트 이벤트 등록 코드
- [x] 음소거 이벤트 등록 코드
- [x] 자동 진행 이벤트 등록 코드
- [x] 로그 이벤트 등록 코드

## 로컬 실행

기존 폴더에 덮어쓰지 말고 새 폴더에서 압축을 해제합니다.

```text
Live Server → /game/
```

브라우저에서는 한 번 강력 새로고침합니다.

```text
Ctrl + F5
```
