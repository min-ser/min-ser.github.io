import {getAreaConfig,getDocuments} from "@/lib/content";import PageHeader from "@/components/content/PageHeader";import MarkdownView from "@/components/content/MarkdownView";export default function Page(){const config=getAreaConfig("07_ARCHIVE"),docs=getDocuments("archive");return <section className="section pageTop"><PageHeader config={config} count={docs.length}/>{docs.map(d=><article className="archiveItem" key={d.slug}><h2>{String(d.meta.title||"")}</h2><MarkdownView content={d.content}/></article>)}</section>}
