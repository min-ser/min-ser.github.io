import type { AreaConfig } from "@/types/content"; export default function PageHeader({config,count}:{config:AreaConfig;count?:number}){return <><div className="terminalLine"><span>{config.terminalPath}</span>{count!==undefined&&<span>{count} {config.counterLabel}</span>}</div><p className="eyebrow">{config.pageEyebrow}</p><h1>{config.pageTitle}</h1><p className="lead">{config.pageDescription}</p></>}
